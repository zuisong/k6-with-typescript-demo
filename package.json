{
  "name": "k6-demos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm esbuild *.ts --format=esm --outdir=dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/k6": "^0.45.1",
    "k6": "^0.0.0"
  },
  "dependencies": {
    "esbuild": "^0.18.17"
  }
}
